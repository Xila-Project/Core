#![feature(prelude_import)]
#![no_std]
extern crate core;
#[prelude_import]
use core::prelude::rust_2024::*;

extern crate alloc;

mod file_system {


    mod directory {
        use crate::{
            XilaFileSystemAccess, XilaFileSystemResult,
            XilaFileSystemStatistics, XilaTaskIdentifier, abi_unsafe_function,
            parse_c_str,
        };
        use alloc::ffi::CString;
        use core::{ffi::c_char, ptr::null_mut};
        use log::debug;
        use shared::generate_shadow_type;
        use task::{TaskIdentifier, block_on};
        use virtual_file_system::{
            SynchronousDirectory, get_instance as get_file_system_instance,
        };
        use super::{XilaFileKind, XilaFileSystemInode, XilaFileSystemSize};
        /// cbindgen:field-names=[data]
        #[repr(C, align(8))]
        pub struct XilaFileSystemDirectory {
            pub data: [u8; 40],
        }
        const _: () =
            {
                if !(::core::mem::size_of::<SynchronousDirectory>() == 40) {
                    ::core::panicking::panic("assertion failed: ::core::mem::size_of::<SynchronousDirectory>() == 40")
                };
                if !(::core::mem::align_of::<SynchronousDirectory>() == 8) {
                    ::core::panicking::panic("assertion failed: ::core::mem::align_of::<SynchronousDirectory>() == 8")
                };
                if !(::core::mem::size_of::<XilaFileSystemDirectory>() == 40)
                    {
                    ::core::panicking::panic("assertion failed: ::core::mem::size_of::<XilaFileSystemDirectory>() == 40")
                };
                if !(::core::mem::align_of::<XilaFileSystemDirectory>() == 8)
                    {
                    ::core::panicking::panic("assertion failed: ::core::mem::align_of::<XilaFileSystemDirectory>() == 8")
                };
            };
        impl XilaFileSystemDirectory {
            #[inline]
            pub fn from_real(real: SynchronousDirectory) -> Self {
                unsafe {
                    let mut proxy = ::core::mem::MaybeUninit::<Self>::uninit();
                    ::core::ptr::write(proxy.as_mut_ptr().cast::<SynchronousDirectory>(),
                        real);
                    proxy.assume_init()
                }
            }
            /// # Safety: must have been initialized via `from_real`
            #[inline]
            pub unsafe fn into_real(self) -> SynchronousDirectory {
                let real =
                    ::core::ptr::read(self.data.as_ptr().cast::<SynchronousDirectory>());
                ::core::mem::forget(self);
                real
            }
            /// # Safety: must have been initialized via `from_real`
            #[inline]
            pub unsafe fn as_real(&self) -> &SynchronousDirectory {
                &*self.data.as_ptr().cast::<SynchronousDirectory>()
            }
            /// # Safety: must have been initialized via `from_real`
            #[inline]
            pub unsafe fn as_real_mut(&mut self)
                -> &mut SynchronousDirectory {
                &mut *self.data.as_mut_ptr().cast::<SynchronousDirectory>()
            }
        }
        impl ::core::ops::Deref for XilaFileSystemDirectory {
            type Target = SynchronousDirectory;
            fn deref(&self) -> &SynchronousDirectory {
                unsafe { self.as_real() }
            }
        }
        impl ::core::ops::DerefMut for XilaFileSystemDirectory {
            fn deref_mut(&mut self) -> &mut SynchronousDirectory {
                unsafe { self.as_real_mut() }
            }
        }
        #[doc = r" This function is used to open a directory."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_directory_open(task:
                XilaTaskIdentifier, path: *const c_char,
            out_directory: *mut XilaFileSystemDirectory)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_directory_open"), lvl,
                            &("abi_definitions::file_system::directory",
                                    "abi_definitions::file_system::directory",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let path = parse_c_str(path)?;
                                    let task = TaskIdentifier::from(task);
                                    {
                                        {
                                            let lvl = ::log::Level::Debug;
                                            if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                    lvl <= ::log::max_level() {
                                                ::log::__private_api::log({
                                                        ::log::__private_api::GlobalLogger
                                                    },
                                                    format_args!("Opening directory {0:?} for task {1:?}", path,
                                                        task), lvl,
                                                    &("abi_definitions::file_system::directory",
                                                            "abi_definitions::file_system::directory",
                                                            ::log::__private_api::loc()), ());
                                            }
                                        }
                                    };
                                    let synchronous_directory =
                                        SynchronousDirectory::open(get_file_system_instance(), task,
                                                path)?;
                                    *out_directory =
                                        XilaFileSystemDirectory::from_real(synchronous_directory);
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_directory_open", error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to read a directory."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_directory_read(directory:
                *mut XilaFileSystemDirectory, entry_name: *mut *const c_char,
            entry_type: *mut XilaFileKind,
            entry_size: *mut XilaFileSystemSize,
            entry_inode: *mut XilaFileSystemInode) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_directory_read"), lvl,
                            &("abi_definitions::file_system::directory",
                                    "abi_definitions::file_system::directory",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    {
                                        {
                                            let lvl = ::log::Level::Debug;
                                            if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                    lvl <= ::log::max_level() {
                                                ::log::__private_api::log({
                                                        ::log::__private_api::GlobalLogger
                                                    }, format_args!("Reading directory {0:?}", directory), lvl,
                                                    &("abi_definitions::file_system::directory",
                                                            "abi_definitions::file_system::directory",
                                                            ::log::__private_api::loc()), ());
                                            }
                                        }
                                    };
                                    let entry = (*directory).read()?;
                                    if let Some(entry) = entry {
                                        *entry_name =
                                            CString::new(entry.name.as_str()).unwrap().into_raw();
                                        *entry_type = entry.kind.into();
                                        *entry_size = entry.size;
                                        *entry_inode = entry.inode;
                                    } else { *entry_name = null_mut(); }
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_directory_read", error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_directory_close(directory:
                *mut XilaFileSystemDirectory) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_directory_close"), lvl,
                            &("abi_definitions::file_system::directory",
                                    "abi_definitions::file_system::directory",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                { (*directory).close_internal(get_file_system_instance()) }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_directory_close", error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_directory_rewind(directory:
                *mut XilaFileSystemDirectory) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_directory_rewind"), lvl,
                            &("abi_definitions::file_system::directory",
                                    "abi_definitions::file_system::directory",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    {
                                        {
                                            let lvl = ::log::Level::Debug;
                                            if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                    lvl <= ::log::max_level() {
                                                ::log::__private_api::log({
                                                        ::log::__private_api::GlobalLogger
                                                    }, format_args!("Rewinding directory {0:?} ", directory),
                                                    lvl,
                                                    &("abi_definitions::file_system::directory",
                                                            "abi_definitions::file_system::directory",
                                                            ::log::__private_api::loc()), ());
                                            }
                                        }
                                    };
                                    (*directory).rewind()
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_directory_rewind", error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_directory_set_position(directory:
                *mut XilaFileSystemDirectory, offset: XilaFileSystemSize)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_directory_set_position"), lvl,
                            &("abi_definitions::file_system::directory",
                                    "abi_definitions::file_system::directory",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    {
                                        {
                                            let lvl = ::log::Level::Debug;
                                            if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                    lvl <= ::log::max_level() {
                                                ::log::__private_api::log({
                                                        ::log::__private_api::GlobalLogger
                                                    },
                                                    format_args!("Setting position in directory {0:?} to offset {1}",
                                                        directory, offset), lvl,
                                                    &("abi_definitions::file_system::directory",
                                                            "abi_definitions::file_system::directory",
                                                            ::log::__private_api::loc()), ());
                                            }
                                        }
                                    };
                                    (*directory).set_position(offset)?;
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_directory_set_position", error, non_zero),
                                        lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_directory_get_statistics(directory:
                *mut XilaFileSystemDirectory,
            statistics: *mut XilaFileSystemStatistics)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_directory_get_statistics"), lvl,
                            &("abi_definitions::file_system::directory",
                                    "abi_definitions::file_system::directory",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    {
                                        {
                                            let lvl = ::log::Level::Debug;
                                            if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                    lvl <= ::log::max_level() {
                                                ::log::__private_api::log({
                                                        ::log::__private_api::GlobalLogger
                                                    },
                                                    format_args!("Getting statistics for directory {0:?}",
                                                        directory), lvl,
                                                    &("abi_definitions::file_system::directory",
                                                            "abi_definitions::file_system::directory",
                                                            ::log::__private_api::loc()), ());
                                            }
                                        }
                                    };
                                    let result = (*directory).get_statistics()?;
                                    *statistics =
                                        XilaFileSystemStatistics::from_statistics(result);
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_directory_get_statistics", error,
                                            non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_directory_get_access(directory:
                *mut XilaFileSystemDirectory,
            access: *mut XilaFileSystemAccess) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_directory_get_access"), lvl,
                            &("abi_definitions::file_system::directory",
                                    "abi_definitions::file_system::directory",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    {
                                        {
                                            let lvl = ::log::Level::Debug;
                                            if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                    lvl <= ::log::max_level() {
                                                ::log::__private_api::log({
                                                        ::log::__private_api::GlobalLogger
                                                    },
                                                    format_args!("Getting access flags for directory {0:?}",
                                                        directory), lvl,
                                                    &("abi_definitions::file_system::directory",
                                                            "abi_definitions::file_system::directory",
                                                            ::log::__private_api::loc()), ());
                                            }
                                        }
                                    };
                                    let result = (*directory).get_access()?;
                                    access.write(result.bits());
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_directory_get_access", error, non_zero),
                                        lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_directory_get_state(directory:
                *mut XilaFileSystemDirectory, state: *mut u8)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_directory_get_state"), lvl,
                            &("abi_definitions::file_system::directory",
                                    "abi_definitions::file_system::directory",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    {
                                        {
                                            let lvl = ::log::Level::Debug;
                                            if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                    lvl <= ::log::max_level() {
                                                ::log::__private_api::log({
                                                        ::log::__private_api::GlobalLogger
                                                    },
                                                    format_args!("Getting state flags for directory {0:?}",
                                                        directory), lvl,
                                                    &("abi_definitions::file_system::directory",
                                                            "abi_definitions::file_system::directory",
                                                            ::log::__private_api::loc()), ());
                                            }
                                        }
                                    };
                                    let result = (*directory).get_state()?;
                                    state.write(result.bits());
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_directory_get_state", error, non_zero),
                                        lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to get the position in a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_directory_create(task:
                XilaTaskIdentifier, path: *const c_char)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_directory_create"), lvl,
                            &("abi_definitions::file_system::directory",
                                    "abi_definitions::file_system::directory",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let path = parse_c_str(path)?;
                                    block_on(get_file_system_instance().create_directory(task.into(),
                                                &path))?;
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_directory_create", error, non_zero), lvl,
                                        &("abi_definitions::file_system::directory",
                                                "abi_definitions::file_system::directory",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
    }
    mod file {
        /// This module implements the POSIX like file system C ABI.
        use core::ffi::c_char;
        use file_system::{
            AccessFlags, CreateFlags, Flags, Kind, Permissions, StateFlags,
            Statistics, Time, character_device,
        };
        use shared::generate_shadow_type;
        use users::{GroupIdentifier, UserIdentifier};
        use virtual_file_system::{
            Error, SynchronousFile, get_instance as get_file_system_instance,
        };
        use crate::{
            XilaFileSystemPollEvent, XilaFileSystemState, XilaTaskIdentifier,
            XilaTime, abi_unsafe_function, file_system::into_position,
            into_result, parse_c_str,
        };
        use super::{
            XilaFileIdentifier, XilaFileSystemAccess, XilaFileSystemOpen,
            XilaFileSystemResult, XilaFileSystemSize,
            XilaFileSystemStatistics, XilaFileSystemWhence,
        };
        /// cbindgen:field-names=[data]
        #[repr(C, align(8))]
        pub struct XilaFileSystemFile {
            pub data: [u8; 56],
        }
        const _: () =
            {
                if !(::core::mem::size_of::<SynchronousFile>() == 56) {
                    ::core::panicking::panic("assertion failed: ::core::mem::size_of::<SynchronousFile>() == 56")
                };
                if !(::core::mem::align_of::<SynchronousFile>() == 8) {
                    ::core::panicking::panic("assertion failed: ::core::mem::align_of::<SynchronousFile>() == 8")
                };
                if !(::core::mem::size_of::<XilaFileSystemFile>() == 56) {
                    ::core::panicking::panic("assertion failed: ::core::mem::size_of::<XilaFileSystemFile>() == 56")
                };
                if !(::core::mem::align_of::<XilaFileSystemFile>() == 8) {
                    ::core::panicking::panic("assertion failed: ::core::mem::align_of::<XilaFileSystemFile>() == 8")
                };
            };
        impl XilaFileSystemFile {
            #[inline]
            pub fn from_real(real: SynchronousFile) -> Self {
                unsafe {
                    let mut proxy = ::core::mem::MaybeUninit::<Self>::uninit();
                    ::core::ptr::write(proxy.as_mut_ptr().cast::<SynchronousFile>(),
                        real);
                    proxy.assume_init()
                }
            }
            /// # Safety: must have been initialized via `from_real`
            #[inline]
            pub unsafe fn into_real(self) -> SynchronousFile {
                let real =
                    ::core::ptr::read(self.data.as_ptr().cast::<SynchronousFile>());
                ::core::mem::forget(self);
                real
            }
            /// # Safety: must have been initialized via `from_real`
            #[inline]
            pub unsafe fn as_real(&self) -> &SynchronousFile {
                &*self.data.as_ptr().cast::<SynchronousFile>()
            }
            /// # Safety: must have been initialized via `from_real`
            #[inline]
            pub unsafe fn as_real_mut(&mut self) -> &mut SynchronousFile {
                &mut *self.data.as_mut_ptr().cast::<SynchronousFile>()
            }
        }
        impl ::core::ops::Deref for XilaFileSystemFile {
            type Target = SynchronousFile;
            fn deref(&self) -> &SynchronousFile { unsafe { self.as_real() } }
        }
        impl ::core::ops::DerefMut for XilaFileSystemFile {
            fn deref_mut(&mut self) -> &mut SynchronousFile {
                unsafe { self.as_real_mut() }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_get_statistics(file:
                *mut XilaFileSystemFile,
            statistics: *mut XilaFileSystemStatistics)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_get_statistics"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    {
                                        {
                                            let lvl = ::log::Level::Debug;
                                            if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                    lvl <= ::log::max_level() {
                                                ::log::__private_api::log({
                                                        ::log::__private_api::GlobalLogger
                                                    }, format_args!("Getting statistics for file {0:?}", file),
                                                    lvl,
                                                    &("abi_definitions::file_system::file",
                                                            "abi_definitions::file_system::file",
                                                            ::log::__private_api::loc()), ());
                                            }
                                        }
                                    };
                                    let result =
                                        match (*file).get_statistics() {
                                            Ok(statistics) => statistics,
                                            Err(Error::UnsupportedOperation) |
                                                Err(Error::FileSystem(file_system::Error::UnsupportedOperation))
                                                => {
                                                {
                                                    {
                                                        let lvl = ::log::Level::Warn;
                                                        if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                                lvl <= ::log::max_level() {
                                                            ::log::__private_api::log({
                                                                    ::log::__private_api::GlobalLogger
                                                                },
                                                                format_args!("File system does not support getting statistics for file {0:?}, returning synthetic character device metadata",
                                                                    file), lvl,
                                                                &("abi_definitions::file_system::file",
                                                                        "abi_definitions::file_system::file",
                                                                        ::log::__private_api::loc()), ());
                                                        }
                                                    }
                                                };
                                                Statistics::new(0, 1, 0, Time::new(0), Time::new(0),
                                                    Time::new(0), Time::new(0), Kind::CharacterDevice,
                                                    Permissions::DEVICE_DEFAULT, UserIdentifier::ROOT,
                                                    GroupIdentifier::ROOT)
                                            }
                                            Err(error) => return Err(error),
                                        };
                                    *statistics =
                                        XilaFileSystemStatistics::from_statistics(result);
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_get_statistics", error, non_zero),
                                        lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to get the access mode of a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[doc = r""]
        #[doc = r" # Errors"]
        #[doc = r""]
        #[doc =
        r" This function may return an error if the file system fails to get the access mode of the file."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_get_access_flags(file:
                *mut XilaFileSystemFile, mode: *mut XilaFileSystemAccess)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_get_access_flags"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let m = (*file).get_access()?;
                                    mode.write(m.bits());
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_get_access_flags", error, non_zero),
                                        lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to close a file."]
        #[doc = r""]
        #[doc = r" # Errors"]
        #[doc = r""]
        #[doc =
        r" This function may return an error if the file system fails to close the file."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_close(file:
                *mut XilaFileSystemFile) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_close"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                { (*file).close_internal(get_file_system_instance()) }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_close", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc =
        r" This function is used perform a vectored write operation on a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[doc = r""]
        #[doc = r" # Errors"]
        #[doc = r""]
        #[doc =
        r" This function may return an error if the file system fails to open the file."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_write(file:
                *mut XilaFileSystemFile, buffers: *const *const u8,
            buffers_length: *const usize, buffer_count: usize,
            written: *mut usize) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_write"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let buffers =
                                        core::slice::from_raw_parts(buffers, buffer_count);
                                    let buffers_length =
                                        core::slice::from_raw_parts(buffers_length, buffer_count);
                                    let mut current_written = 0;
                                    for (buffer, length) in
                                        buffers.iter().zip(buffers_length.iter()) {
                                        let buffer_slice =
                                            core::slice::from_raw_parts(*buffer, *length);
                                        current_written += (*file).write(buffer_slice)?;
                                    }
                                    if !written.is_null() { *written = current_written; }
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_write", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc =
        r" This function is used to perform a write operation on a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[doc = r""]
        #[doc = r" # Errors"]
        #[doc = r""]
        #[doc =
        r" This function may return an error if the file system fails to open the file."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_read(file:
                *mut XilaFileSystemFile, buffers: *mut *mut u8,
            buffers_length: *mut usize, buffer_count: usize, read: *mut usize)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_read"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let buffers =
                                        core::slice::from_raw_parts_mut(buffers, buffer_count);
                                    let buffers_length =
                                        core::slice::from_raw_parts_mut(buffers_length,
                                            buffer_count);
                                    let mut current_read = 0;
                                    for (buffer_pointer, buffer_length) in
                                        buffers.iter_mut().zip(buffers_length.iter_mut()) {
                                        let buffer =
                                            core::slice::from_raw_parts_mut(*buffer_pointer,
                                                *buffer_length);
                                        let read_count = (*file).read(buffer)?;
                                        current_read += read_count;
                                    }
                                    if !read.is_null() { *read = current_read; }
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_read", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc =
        r" This function is used to perform a read operation on a file at a specific position."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_read_at(file:
                *mut XilaFileSystemFile, buffers: *mut *mut u8,
            buffers_length: *mut usize, buffer_count: usize, position: u64,
            read: *mut usize) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_read_at"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let buffers =
                                        core::slice::from_raw_parts_mut(buffers, buffer_count);
                                    let buffers_length =
                                        core::slice::from_raw_parts_mut(buffers_length,
                                            buffer_count);
                                    (*file).set_position(&file_system::Position::Start(position))?;
                                    let mut current_read = 0;
                                    for (buffer_pointer, buffer_length) in
                                        buffers.iter_mut().zip(buffers_length.iter_mut()) {
                                        let buffer =
                                            core::slice::from_raw_parts_mut(*buffer_pointer,
                                                *buffer_length);
                                        let read_count = (*file).read(buffer)?;
                                        current_read += read_count;
                                    }
                                    if !read.is_null() { *read = current_read; }
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_read_at", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc =
        r" This function is used to perform a write operation on a file at a specific position."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_write_at(file:
                *mut XilaFileSystemFile, buffers: *const *const u8,
            buffers_length: *const usize, buffer_count: usize, position: u64,
            written: *mut usize) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_write_at"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let buffers =
                                        core::slice::from_raw_parts(buffers, buffer_count);
                                    let buffers_length =
                                        core::slice::from_raw_parts(buffers_length, buffer_count);
                                    (*file).set_position(&file_system::Position::Start(position))?;
                                    let mut current_written = 0;
                                    for (buffer, length) in
                                        buffers.iter().zip(buffers_length.iter()) {
                                        let buffer_slice =
                                            core::slice::from_raw_parts(*buffer, *length);
                                        current_written += (*file).write(buffer_slice)?;
                                    }
                                    if !written.is_null() { *written = current_written; }
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_write_at", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to check if a file is a terminal."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[doc = r""]
        #[doc = r" # Errors"]
        #[doc = r""]
        #[doc =
        r" This function may return an error if the file system fails to open the file."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_is_a_terminal(file:
                *mut XilaFileSystemFile, is_a_terminal: *mut bool)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_is_a_terminal"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    *is_a_terminal =
                                        (*file).control(character_device::IS_A_TERMINAL, &())?;
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_is_a_terminal", error, non_zero),
                                        lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to open a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_open(task:
                XilaTaskIdentifier, path: *const c_char,
            mode: XilaFileSystemAccess, open: XilaFileSystemOpen,
            status: XilaFileSystemState, file: *mut XilaFileSystemFile)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_open"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let path = parse_c_str(path)?;
                                    let mode = AccessFlags::from_bits_truncate(mode);
                                    let open = CreateFlags::from_bits_truncate(open);
                                    let status = StateFlags::from_bits_truncate(status);
                                    let flags = Flags::new(mode, Some(open), Some(status));
                                    let f =
                                        SynchronousFile::open(get_file_system_instance(),
                                                task.into(), path, flags)?;
                                    *file = XilaFileSystemFile::from_real(f);
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_open", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_set_flags(_file:
                XilaFileIdentifier, _status: XilaFileSystemState)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_set_flags"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                { ::core::panicking::panic("not yet implemented") }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_set_flags", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to get the flags of a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_get_state(file:
                *mut XilaFileSystemFile, state: *mut XilaFileSystemState)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_get_state"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let s = (*file).get_state()?;
                                    state.write(s.bits());
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_get_state", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_flush(file:
                *mut XilaFileSystemFile, _t: bool) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_flush"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (|| unsafe { { (*file).flush() } })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_flush", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to set the position in a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_file_set_position(file:
                *mut XilaFileSystemFile, offset: i64,
            whence: XilaFileSystemWhence, position: *mut XilaFileSystemSize)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_file_set_position"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let current_position = into_position(whence, offset);
                                    let result = (*file).set_position(&current_position)?;
                                    *position = result;
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_file_set_position", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_set_times(_file:
                XilaFileIdentifier, _access: XilaTime,
            _modification: XilaTime, _flags: u8) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_set_times"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                { ::core::panicking::panic("not yet implemented") }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_set_times", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc =
        r" This function is used to set access and modification times of a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_set_times_from_path(_path:
                *const c_char, _access: XilaTime, _modification: XilaTime,
            _flags: u8, _follow: bool) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_set_times_from_path"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                { ::core::panicking::panic("not yet implemented") }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_set_times_from_path", error, non_zero),
                                        lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to truncate a file."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_truncate(_file:
                XilaFileIdentifier, _length: XilaFileSystemSize)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_truncate"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                { ::core::panicking::panic("not yet implemented") }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_truncate", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to create a symbolic link."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_link(_path: *const c_char,
            _link: *const c_char) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_link"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                { ::core::panicking::panic("not yet implemented") }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_link", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc =
        r" This function is used to advice the file system about the access pattern of a file."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_advise(_file:
                XilaFileIdentifier, _offset: XilaFileSystemSize,
            _length: XilaFileSystemSize, _advice: u8)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_advise"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                { ::core::panicking::panic("not yet implemented") }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_advise", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_allocate(_file:
                XilaFileIdentifier, _offset: XilaFileSystemSize,
            _length: XilaFileSystemSize) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_allocate"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                { ::core::panicking::panic("not yet implemented") }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_allocate", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_dummy(_event:
                XilaFileSystemPollEvent) -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_dummy"), lvl,
                            &("abi_definitions::file_system::file",
                                    "abi_definitions::file_system::file",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (|| unsafe { { Ok(()) } })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_dummy", error, non_zero), lvl,
                                        &("abi_definitions::file_system::file",
                                                "abi_definitions::file_system::file",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
    }
    mod socket {
        use crate::abi_unsafe_function;
        use super::{XilaFileIdentifier, XilaFileSystemResult, into_result};
        use task::block_on;
        use task::get_instance as get_task_manager_instance;
        use virtual_file_system::Error;
        #[doc = r" This function is used to send data through a socket."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_network_send(_socket:
                XilaFileIdentifier, buffer: *const u8, size: usize)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}", "xila_network_send"),
                            lvl,
                            &("abi_definitions::file_system::socket",
                                    "abi_definitions::file_system::socket",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    if buffer.is_null() { return Err(Error::InvalidParameter); }
                                    let _buffer = core::slice::from_raw_parts(buffer, size);
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::socket",
                                                "abi_definitions::file_system::socket",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_network_send", error, non_zero), lvl,
                                        &("abi_definitions::file_system::socket",
                                                "abi_definitions::file_system::socket",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to receive data through a socket."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[doc = r""]
        #[doc = r" # Errors"]
        #[doc = r""]
        #[doc =
        r" This function may return an error if the file system fails to receive the data."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_network_receive(_socket:
                XilaFileIdentifier, buffer: *mut u8, size: usize)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_network_receive"), lvl,
                            &("abi_definitions::file_system::socket",
                                    "abi_definitions::file_system::socket",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    if buffer.is_null() { return Err(Error::InvalidParameter); }
                                    let _buffer = core::slice::from_raw_parts_mut(buffer, size);
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::socket",
                                                "abi_definitions::file_system::socket",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_network_receive", error, non_zero), lvl,
                                        &("abi_definitions::file_system::socket",
                                                "abi_definitions::file_system::socket",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
    }
    mod types {
        use file_system::{
            AccessFlags, CreateFlags, Kind, Position, StateFlags,
        };
        use crate::{XilaGroupIdentifier, XilaTime, XilaUserIdentifier};
        #[repr(u8)]
        pub enum XilaFileSystemWhence { Start, Current, End, }
        #[automatically_derived]
        impl ::core::fmt::Debug for XilaFileSystemWhence {
            #[inline]
            fn fmt(&self, f: &mut ::core::fmt::Formatter)
                -> ::core::fmt::Result {
                ::core::fmt::Formatter::write_str(f,
                    match self {
                        XilaFileSystemWhence::Start => "Start",
                        XilaFileSystemWhence::Current => "Current",
                        XilaFileSystemWhence::End => "End",
                    })
            }
        }
        #[automatically_derived]
        #[doc(hidden)]
        unsafe impl ::core::clone::TrivialClone for XilaFileSystemWhence { }
        #[automatically_derived]
        impl ::core::clone::Clone for XilaFileSystemWhence {
            #[inline]
            fn clone(&self) -> XilaFileSystemWhence { *self }
        }
        #[automatically_derived]
        impl ::core::marker::Copy for XilaFileSystemWhence { }
        #[automatically_derived]
        impl ::core::marker::StructuralPartialEq for XilaFileSystemWhence { }
        #[automatically_derived]
        impl ::core::cmp::PartialEq for XilaFileSystemWhence {
            #[inline]
            fn eq(&self, other: &XilaFileSystemWhence) -> bool {
                let __self_discr =
                    ::core::intrinsics::discriminant_value(self);
                let __arg1_discr =
                    ::core::intrinsics::discriminant_value(other);
                __self_discr == __arg1_discr
            }
        }
        #[automatically_derived]
        impl ::core::cmp::Eq for XilaFileSystemWhence {
            #[doc(hidden)]
            #[coverage(off)]
            fn assert_fields_are_eq(&self) {}
        }
        pub const fn into_position(whence: XilaFileSystemWhence, offset: i64)
            -> Position {
            match whence {
                XilaFileSystemWhence::Start => Position::Start(offset as u64),
                XilaFileSystemWhence::Current => Position::Current(offset),
                XilaFileSystemWhence::End => Position::End(offset),
            }
        }
        #[repr(u8)]
        pub enum XilaFileKind {
            File,
            Directory,
            BlockDevice,
            CharacterDevice,
            Pipe,
            Socket,
            SymbolicLink,
        }
        impl From<XilaFileKind> for file_system::Kind {
            fn from(type_value: XilaFileKind) -> Self {
                match type_value {
                    XilaFileKind::File => Kind::File,
                    XilaFileKind::Directory => file_system::Kind::Directory,
                    XilaFileKind::BlockDevice => file_system::Kind::BlockDevice,
                    XilaFileKind::CharacterDevice =>
                        file_system::Kind::CharacterDevice,
                    XilaFileKind::Pipe => file_system::Kind::Pipe,
                    XilaFileKind::Socket => file_system::Kind::Socket,
                    XilaFileKind::SymbolicLink =>
                        file_system::Kind::SymbolicLink,
                }
            }
        }
        impl From<file_system::Kind> for XilaFileKind {
            fn from(type_value: file_system::Kind) -> Self {
                match type_value {
                    file_system::Kind::File => XilaFileKind::File,
                    file_system::Kind::Directory => XilaFileKind::Directory,
                    file_system::Kind::BlockDevice => XilaFileKind::BlockDevice,
                    file_system::Kind::CharacterDevice =>
                        XilaFileKind::CharacterDevice,
                    file_system::Kind::Pipe => XilaFileKind::Pipe,
                    file_system::Kind::Socket => XilaFileKind::Socket,
                    file_system::Kind::SymbolicLink =>
                        XilaFileKind::SymbolicLink,
                }
            }
        }
        pub type XilaFileSystemAccess = u8;
        #[unsafe(no_mangle)]
        pub static XILA_FILE_SYSTEM_ACCESS_READ_MASK: u8 =
            AccessFlags::Read.bits();
        #[unsafe(no_mangle)]
        pub static XILA_FILE_SYSTEM_ACCESS_WRITE_MASK: u8 =
            AccessFlags::Write.bits();
        pub type XilaFileSystemOpen = u8;
        #[unsafe(no_mangle)]
        pub static XILA_FILE_SYSTEM_OPEN_CREATE_MASK: u8 =
            CreateFlags::Create.bits();
        #[unsafe(no_mangle)]
        pub static XILA_FILE_SYSTEM_OPEN_CREATE_ONLY_MASK: u8 =
            CreateFlags::Exclusive.bits();
        #[unsafe(no_mangle)]
        pub static XILA_FILE_SYSTEM_OPEN_TRUNCATE_MASK: u8 =
            CreateFlags::Truncate.bits();
        pub type XilaFileSystemState = u8;
        #[unsafe(no_mangle)]
        pub static XILA_FILE_SYSTEM_STATE_APPEND_MASK: u8 =
            StateFlags::Append.bits();
        #[unsafe(no_mangle)]
        pub static XILA_FILE_SYSTEM_STATE_NON_BLOCKING_MASK: u8 =
            StateFlags::NonBlocking.bits();
        #[unsafe(no_mangle)]
        pub static XILA_FILE_SYSTEM_STATE_SYNCHRONOUS_MASK: u8 =
            StateFlags::Synchronous.bits();
        #[unsafe(no_mangle)]
        pub static XILA_FILE_SYSTEM_STATE_SYNCHRONOUS_DATA_ONLY_MASK: u8 =
            StateFlags::SynchronousDataOnly.bits();
        pub type XilaFileSystemInode = u64;
        type XilaFileSystemIdentifier = u32;
        type Permissions = u16;
        #[repr(C)]
        pub struct XilaFileSystemStatistics {
            file_system: XilaFileSystemIdentifier,
            inode: XilaFileSystemInode,
            links: u64,
            size: XilaFileSystemSize,
            creation: XilaTime,
            access: XilaTime,
            modification: XilaTime,
            status: XilaTime,
            kind: XilaFileKind,
            permissions: Permissions,
            user: XilaUserIdentifier,
            group: XilaGroupIdentifier,
        }
        impl XilaFileSystemStatistics {
            pub fn from_statistics(statistics: file_system::Statistics)
                -> Self {
                Self {
                    file_system: 0,
                    inode: statistics.inode,
                    links: statistics.links,
                    size: statistics.size,
                    creation: statistics.creation.as_u64(),
                    access: statistics.access.as_u64(),
                    modification: statistics.modification.as_u64(),
                    status: statistics.status.as_u64(),
                    kind: statistics.kind.into(),
                    permissions: statistics.permissions.as_u16(),
                    user: statistics.user.as_u16(),
                    group: statistics.user.as_u16(),
                }
            }
            /// # Safety
            ///
            /// This function is unsafe because it dereferences a raw pointer.
            /// The caller must ensure that the pointer is valid and properly aligned.
            pub unsafe fn from_mutable_pointer<'a>(pointer:
                    *mut XilaFileSystemStatistics)
                -> Option<&'a mut XilaFileSystemStatistics> {
                if pointer.is_null() { return None; }
                if !(pointer as
                                    usize).is_multiple_of(align_of::<XilaFileSystemStatistics>())
                    {
                    return None;
                }
                Some(unsafe { &mut *pointer })
            }
        }
        pub type XilaFileIdentifier = u16;
        pub type XilaFileSystemSize = u64;
        pub type XilaFileSystemResult = u32;
        pub const XILA_FILE_SYSTEM_RESULT_SUCCESS: u32 = 0;
        #[repr(C)]
        pub struct XilaFileSystemPollEvent {
            fd: XilaFileIdentifier,
            events: u32,
            revents: u32,
        }
    }
    mod common {
        use core::{
            ffi::{CStr, c_char},
            num::NonZeroU32,
        };
        use virtual_file_system::Error;
        use crate::XilaFileSystemResult;
        /// This function is used to convert a function returning a Result into a u32.
        pub fn into_result<F>(function: F) -> XilaFileSystemResult where
            F: FnOnce() -> Result<(), virtual_file_system::Error> {
            match function() {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            Error::RessourceBusy |
                                Error::FileSystem(file_system::Error::RessourceBusy) =>
                                true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::common",
                                                "abi_definitions::file_system::common",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error: {0:?} ({1})", error,
                                            non_zero), lvl,
                                        &("abi_definitions::file_system::common",
                                                "abi_definitions::file_system::common",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[inline]
        pub unsafe fn parse_c_str(path: *const c_char)
            -> Result<&'static str, Error> {
            if path.is_null() { return Err(Error::InvalidParameter); }
            unsafe {
                CStr::from_ptr(path).to_str().map_err(|_|
                        Error::InvalidParameter)
            }
        }
        #[macro_export]
        macro_rules! abi_unsafe_function {
            ($(#[$meta:meta])* fn $name:ident($($arg:ident : $ty:ty),* $(,)?)
            -> XilaFileSystemResult $body:block) =>
            {
                $(#[$meta])* #[unsafe(no_mangle)] pub unsafe extern "C" fn
                $name($($arg : $ty),*) -> XilaFileSystemResult
                {
                    log::debug!("Calling function {}", stringify!($name));
                    #[allow(unused_unsafe)] let result: Result<(),
                    virtual_file_system::Error> = (|| unsafe { $body })(); match
                    result
                    {
                        Ok(()) => 0, Err(error) =>
                        {
                            let non_zero: core::num::NonZeroU32 = error.into(); if
                            matches!(error, virtual_file_system::Error::RessourceBusy |
                            virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy))
                            {
                                log::debug!("File system busy (expected while polling): {:?} ({})",
                                error, non_zero);
                            } else
                            {
                                log::error!("File system error when calling {}: {:?} ({})",
                                stringify!($name), error, non_zero);
                            } non_zero.get()
                        }
                    }
                }
            };
        }
    }
    mod path {
        use core::{
            ffi::{CStr, c_char},
            num::NonZeroU32, ptr::copy_nonoverlapping,
        };
        use task::block_on;
        use virtual_file_system::Error;
        use crate::{
            XilaFileSystemResult, XilaFileSystemStatistics,
            XilaTaskIdentifier, abi_unsafe_function, into_result, parse_c_str,
        };
        #[doc = r" This function is used to get statistics for a path."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_get_statistics_from_path(path:
                *const c_char, statistics: *mut XilaFileSystemStatistics)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_get_statistics_from_path"), lvl,
                            &("abi_definitions::file_system::path",
                                    "abi_definitions::file_system::path",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let path = parse_c_str(path)?;
                                    {
                                        {
                                            let lvl = ::log::Level::Debug;
                                            if lvl <= ::log::STATIC_MAX_LEVEL &&
                                                    lvl <= ::log::max_level() {
                                                ::log::__private_api::log({
                                                        ::log::__private_api::GlobalLogger
                                                    }, format_args!("Getting statistics for path {0:?}", path),
                                                    lvl,
                                                    &("abi_definitions::file_system::path",
                                                            "abi_definitions::file_system::path",
                                                            ::log::__private_api::loc()), ());
                                            }
                                        }
                                    };
                                    let result =
                                        block_on(virtual_file_system::get_instance().get_statistics(&path))?;
                                    *statistics =
                                        XilaFileSystemStatistics::from_statistics(result);
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::path",
                                                "abi_definitions::file_system::path",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_get_statistics_from_path", error,
                                            non_zero), lvl,
                                        &("abi_definitions::file_system::path",
                                                "abi_definitions::file_system::path",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc =
        r" This function is used to convert a path to a resolved path (i.e. a path without symbolic links or relative paths)."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_resolve_path(path:
                *const i8, resolved_path: *mut u8, resolved_path_size: usize)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_resolve_path"), lvl,
                            &("abi_definitions::file_system::path",
                                    "abi_definitions::file_system::path",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let path = parse_c_str(path as *const c_char)?;
                                    copy_nonoverlapping(path.as_ptr(), resolved_path,
                                        usize::min(resolved_path_size, path.len()));
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::path",
                                                "abi_definitions::file_system::path",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_resolve_path", error, non_zero), lvl,
                                        &("abi_definitions::file_system::path",
                                                "abi_definitions::file_system::path",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to rename (move) a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_rename(old_path:
                *const c_char, new_path: *const c_char)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_rename"), lvl,
                            &("abi_definitions::file_system::path",
                                    "abi_definitions::file_system::path",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let old_path = parse_c_str(old_path)?;
                                    let new_path = parse_c_str(new_path)?;
                                    block_on(virtual_file_system::get_instance().rename(&old_path,
                                                &new_path))?;
                                    Ok(())
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::path",
                                                "abi_definitions::file_system::path",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_rename", error, non_zero), lvl,
                                        &("abi_definitions::file_system::path",
                                                "abi_definitions::file_system::path",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
        #[doc = r" This function is used to remove a file."]
        #[doc = r""]
        #[doc = r" # Safety"]
        #[doc = r""]
        #[doc =
        r" This function is unsafe because it dereferences raw pointers."]
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_file_system_remove(task:
                XilaTaskIdentifier, path: *const c_char)
            -> XilaFileSystemResult {
            {
                {
                    let lvl = ::log::Level::Debug;
                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                            lvl <= ::log::max_level() {
                        ::log::__private_api::log({
                                ::log::__private_api::GlobalLogger
                            },
                            format_args!("Calling function {0}",
                                "xila_file_system_remove"), lvl,
                            &("abi_definitions::file_system::path",
                                    "abi_definitions::file_system::path",
                                    ::log::__private_api::loc()), ());
                    }
                }
            };
            #[allow(unused_unsafe)]
            let result: Result<(), virtual_file_system::Error> =
                (||
                            unsafe {
                                {
                                    let path = parse_c_str(path)?;
                                    block_on(virtual_file_system::get_instance().remove(task.into(),
                                            path))
                                }
                            })();
            match result {
                Ok(()) => 0,
                Err(error) => {
                    let non_zero: core::num::NonZeroU32 = error.into();
                    if #[allow(non_exhaustive_omitted_patterns)] match error {
                            virtual_file_system::Error::RessourceBusy |
                                virtual_file_system::Error::FileSystem(file_system::Error::RessourceBusy)
                                => true,
                            _ => false,
                        } {
                        {
                            {
                                let lvl = ::log::Level::Debug;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system busy (expected while polling): {0:?} ({1})",
                                            error, non_zero), lvl,
                                        &("abi_definitions::file_system::path",
                                                "abi_definitions::file_system::path",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    } else {
                        {
                            {
                                let lvl = ::log::Level::Error;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("File system error when calling {0}: {1:?} ({2})",
                                            "xila_file_system_remove", error, non_zero), lvl,
                                        &("abi_definitions::file_system::path",
                                                "abi_definitions::file_system::path",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                    }
                    non_zero.get()
                }
            }
        }
    }
    pub use directory::*;
    pub use file::*;
    pub use socket::*;
    pub use types::*;
    pub use common::*;
    pub use path::*;
}
mod memory {
    mod allocated {
        use core::{alloc::Layout, ptr::NonNull};
        use alloc::slice;
        use crate::CompactLayout;
        pub struct Allocated {
            pointer: NonNull<u8>,
            padding: u8,
        }
        impl Allocated {
            const HEADER_SIZE: usize = size_of::<CompactLayout>();
            pub const fn get_padding(alignment: usize) -> usize {
                let misalignment = Self::HEADER_SIZE % alignment;
                if misalignment == 0 { 0 } else { alignment - misalignment }
            }
            pub fn get_layout_for_allocation(size: usize, alignment: usize)
                -> Layout {
                let padding = Self::get_padding(alignment);
                let offset = Self::HEADER_SIZE + padding;
                let total_size = offset + size;
                Layout::from_size_align(total_size,
                        alignment).expect("Failed to create allocation layout")
            }
            fn new(pointer: *mut u8, padding: usize) -> Option<Self> {
                Some(Self {
                        pointer: NonNull::new(pointer)?,
                        padding: padding as u8,
                    })
            }
            pub fn from_layout(pointer: *mut u8, layout: &Layout)
                -> Option<Self> {
                let padding = Self::get_padding(layout.align());
                let allocated = Self::new(pointer, padding)?;
                allocated.set_layout(layout);
                Some(allocated)
            }
            /// # Safety
            ///
            /// The provided user pointer must have been obtained from a previous allocation
            /// that stored a CompactLayout before the user pointer.
            pub unsafe fn from_user_pointer(user_pointer: *mut u8)
                -> Option<Self> {
                if user_pointer.is_null() { return None; }
                let layout_ptr =
                    unsafe { user_pointer.sub(size_of::<CompactLayout>()) };
                let layout_bytes =
                    unsafe {
                        slice::from_raw_parts(layout_ptr,
                            size_of::<CompactLayout>())
                    };
                let layout =
                    CompactLayout::from_le_bytes(layout_bytes.try_into().ok()?)?;
                let padding = Self::get_padding(layout.get_alignment());
                let total_header_size = Self::HEADER_SIZE + padding;
                let base_pointer =
                    unsafe { user_pointer.sub(total_header_size) };
                Some(Self {
                        pointer: NonNull::new(base_pointer)?,
                        padding: padding as u8,
                    })
            }
            pub fn get_layout(&self) -> Option<Layout> {
                let layout_pointer =
                    unsafe { self.pointer.as_ptr().add(self.padding as usize) };
                let bytes =
                    unsafe {
                        slice::from_raw_parts(layout_pointer,
                            size_of::<CompactLayout>())
                    };
                let layout =
                    CompactLayout::from_le_bytes(bytes.try_into().expect("Failed to read CompactLayout from pointer"))?;
                layout.into_layout()
            }
            pub fn erase_layout(&self) {
                let layout_pointer =
                    unsafe { self.pointer.as_ptr().add(self.padding as usize) };
                let zero_bytes = [0u8; size_of::<CompactLayout>()];
                unsafe {
                    core::ptr::copy_nonoverlapping(zero_bytes.as_ptr(),
                        layout_pointer, size_of::<CompactLayout>());
                }
            }
            pub fn set_layout(&self, layout: &Layout) {
                let bytes =
                    CompactLayout::from_layout(layout).expect("Failed to convert Layout to CompactLayout").to_le_bytes();
                let layout_pointer =
                    unsafe { self.pointer.as_ptr().add(self.padding as usize) };
                unsafe {
                    core::ptr::copy_nonoverlapping(bytes.as_ptr(),
                        layout_pointer, size_of::<CompactLayout>());
                }
            }
            pub fn get_base_pointer(&self) -> *mut u8 {
                self.pointer.as_ptr()
            }
            pub fn get_user_pointer(&self) -> *mut u8 {
                let offset = self.padding as usize + Self::HEADER_SIZE;
                unsafe { self.pointer.as_ptr().add(offset) }
            }
        }
    }
    mod allocation {
        //! # Memory Management Module
        //!
        //! This module provides low-level memory management functionality for the Xila operating system.
        //! It exposes a C-compatible ABI for memory allocation, deallocation, and memory operations
        //! that can be used by applications and other system components.
        //!
        //! ## Features
        //!
        //! - **Memory Allocation**: Allocate memory blocks with specific size, alignment, and capabilities
        //! - **Memory Deallocation**: Free allocated memory blocks
        //! - **Memory Reallocation**: Resize existing memory blocks
        //! - **Cache Management**: Flush data and instruction caches
        //! - **Memory Protection**: Support for read, write, and execute permissions
        //! - **Memory Capabilities**: Support for executable memory and DMA-capable memory
        //!
        //! ## Safety
        //!
        //! This module provides unsafe functions that directly manipulate memory. Callers must ensure:
        //! - Pointers are valid and properly aligned
        //! - Memory is not accessed after deallocation
        //! - Concurrent access is properly synchronized
        //!
        //! ## Example Usage
        //!
        //! ```c
        //! // Allocate 1024 bytes with 8-byte alignment
        //! void* ptr = xila_memory_allocate(NULL, 1024, 8, 0);
        //! if (ptr != NULL) {
        //!     // Use the memory...
        //!     xila_memory_deallocate(ptr);
        //! }
        //! ```
        #![allow(dead_code)]
        use core::ptr::null_mut;
        use core::{ffi::c_void, ptr::NonNull};
        use log::warning;
        use memory::CapabilityFlags;
        use synchronization::blocking_mutex::raw::CriticalSectionRawMutex;
        use synchronization::mutex::Mutex;
        use task::block_on;
        use crate::Allocated;
        #[repr(C)]
        union MaximumAlignment {
            u: u128,
            f: f64,
            p: *const u8,
        }
        const MAXIMUM_ALIGNMENT: usize = align_of::<MaximumAlignment>();
        /// Memory protection flags that can be combined using bitwise OR.
        /// These flags determine what operations are allowed on memory regions.
        pub type XilaMemoryProtection = u8;
        /// Read permission flag - allows reading from memory
        #[unsafe(no_mangle)]
        pub static XILA_MEMORY_PROTECTION_READ: u8 =
            memory::Protection::READ_BIT;
        /// Write permission flag - allows writing to memory
        #[unsafe(no_mangle)]
        pub static XILA_MEMORY_PROTECTION_WRITE: u8 =
            memory::Protection::WRITE_BIT;
        /// Execute permission flag - allows executing code from memory
        #[unsafe(no_mangle)]
        pub static XILA_MEMORY_PROTECTION_EXECUTE: u8 =
            memory::Protection::EXECUTE_BIT;
        /// Memory capability flags that specify special requirements for allocated memory.
        /// These flags can be combined using bitwise OR to request multiple capabilities.
        pub type XilaMemoryCapabilities = u8;
        /// Executable capability - memory can be used for code execution
        #[unsafe(no_mangle)]
        pub static XILA_MEMORY_CAPABILITIES_EXECUTE: XilaMemoryCapabilities =
            memory::CapabilityFlags::Executable.bits();
        /// Direct Memory Access (DMA) capability - memory is accessible by DMA controllers
        #[unsafe(no_mangle)]
        pub static XILA_MEMORY_CAPABILITIES_DIRECT_MEMORY_ACCESS:
            XilaMemoryCapabilities =
            memory::CapabilityFlags::DirectMemoryAccess.bits();
        /// No special capabilities required - standard memory allocation
        #[unsafe(no_mangle)]
        pub static XILA_MEMORY_CAPABILITIES_NONE: XilaMemoryCapabilities = 0;
        static ALLOCATION_MUTEX: Mutex<CriticalSectionRawMutex, ()> =
            Mutex::new(());
        /// Converts a function that returns an `Option<NonNull<P>>` into a raw C-compatible pointer.
        ///
        /// This utility function is used internally to convert Rust's safe pointer types
        /// into C-compatible raw pointers. Returns `null_mut()` if the function returns `None`.
        ///
        /// # Type Parameters
        ///
        /// * `F` - A function that returns `Option<NonNull<P>>`
        /// * `P` - The pointer type being converted
        ///
        /// # Parameters
        ///
        /// * `Function` - The function to execute and convert the result
        ///
        /// # Returns
        ///
        /// A raw C-compatible pointer, or null if the function returns `None`
        pub fn into_pointer<F, P>(function: F) -> *mut c_void where
            F: FnOnce() -> Option<NonNull<P>> {
            match function() {
                Some(pointer) => pointer.as_ptr() as *mut c_void,
                None => null_mut(),
            }
        }
        /// Deallocates a previously allocated memory block.
        ///
        /// This function frees memory that was previously allocated using `xila_memory_allocate`
        /// or `xila_memory_reallocate`. It's safe to call this function with a null pointer.
        ///
        /// # Safety
        ///
        /// - The pointer must have been returned by a previous call to `xila_memory_allocate`
        ///   or `xila_memory_reallocate`
        /// - The pointer must not be used after this function returns
        ///
        /// # Parameters
        ///
        /// * `Pointer` - Pointer to the memory block to deallocate, or null
        ///
        /// # Examples
        ///
        /// ```c
        /// void* ptr = xila_memory_allocate(NULL, 1024, 8, 0);
        /// xila_memory_deallocate(ptr); // Free the memory
        /// xila_memory_deallocate(NULL); // Safe - ignored
        /// ```
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_memory_deallocate(pointer:
                *mut c_void) {
            let _lock = block_on(ALLOCATION_MUTEX.lock());
            let allocated =
                match unsafe {
                        Allocated::from_user_pointer(pointer as *mut u8)
                    } {
                    Some(alloc) => alloc,
                    None => { return; }
                };
            let layout = allocated.get_layout().unwrap();
            let base_pointer = allocated.get_base_pointer();
            unsafe {
                memory::get_instance().deallocate(base_pointer, layout);
            }
        }
        /// Reallocates a memory block to a new size.
        ///
        /// This function changes the size of a previously allocated memory block. If the new size
        /// is larger, the additional memory is uninitialized. If the new size is smaller, the
        /// excess memory is freed. The original data is preserved up to the minimum of the old
        /// and new sizes.
        ///
        /// # Safety
        ///
        /// - If `Pointer` is not null, it must have been returned by a previous call to
        ///   `xila_memory_allocate` or `xila_memory_reallocate`
        /// - The pointer must not be used after this function returns (use the returned pointer instead)
        /// - If the function returns null, the original pointer remains valid
        ///
        /// # Parameters
        ///
        /// * `Pointer` - Pointer to the memory block to reallocate, or null for new allocation
        /// * `Size` - New size in bytes (0 is equivalent to deallocation)
        ///
        /// # Returns
        ///
        /// - Pointer to the reallocated memory block
        /// - Null if allocation fails or if size is 0
        /// - If `Pointer` is null, behaves like `xila_memory_allocate` with default alignment
        ///
        /// # Examples
        ///
        /// ```c
        /// // Allocate initial memory
        /// void* ptr = xila_memory_reallocate(NULL, 1024);
        ///
        /// // Expand the memory
        /// ptr = xila_memory_reallocate(ptr, 2048);
        ///
        /// // Shrink the memory
        /// ptr = xila_memory_reallocate(ptr, 512);
        ///
        /// // Free the memory
        /// xila_memory_reallocate(ptr, 0);
        /// ```
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_memory_reallocate(pointer: *mut c_void,
            size: usize) -> *mut c_void {
            let _lock = block_on(ALLOCATION_MUTEX.lock());
            unsafe {
                let pointer = pointer as *mut u8;
                let allocated =
                    match Allocated::from_user_pointer(pointer) {
                        Some(alloc) => alloc,
                        None => {
                            drop(_lock);
                            return xila_memory_allocate_core(size);
                        }
                    };
                let old_layout = allocated.get_layout().unwrap();
                let new_layout =
                    Allocated::get_layout_for_allocation(size,
                        old_layout.align());
                let pointer =
                    memory::get_instance().reallocate(allocated.get_base_pointer(),
                        old_layout, new_layout.size());
                let allocated =
                    match Allocated::from_layout(pointer, &new_layout) {
                        Some(alloc) => alloc,
                        None => {
                            {
                                {
                                    let lvl = ::log::Level::Warn;
                                    if lvl <= ::log::STATIC_MAX_LEVEL &&
                                            lvl <= ::log::max_level() {
                                        ::log::__private_api::log({
                                                ::log::__private_api::GlobalLogger
                                            },
                                            format_args!("xila_memory_reallocate failed to create allocation metadata: New Size: {0}, Old Size: {1}",
                                                size, old_layout.size()), lvl,
                                            &("abi_definitions::memory::allocation",
                                                    "abi_definitions::memory::allocation",
                                                    ::log::__private_api::loc()), ());
                                    }
                                }
                            };
                            return null_mut();
                        }
                    };
                allocated.get_user_pointer() as *mut c_void
            }
        }
        /// Allocates a memory block with specified properties.
        ///
        /// This function allocates a block of memory with the specified size, alignment,
        /// and capabilities. The memory is uninitialized and must be properly initialized
        /// before use.
        ///
        /// # Safety
        ///
        /// This function is unsafe because:
        /// - The returned memory is uninitialized
        /// - The caller must ensure proper deallocation
        /// - The hint address, if providalignment enumed, must be a valid memory address
        ///
        /// # Parameters
        ///
        /// * `Hint_address` - Preferred memory address (hint only, may be ignored), or null
        /// * `Size` - Size of the memory block in bytes
        /// * `Alignment` - Required memory alignment in bytes (must be a power of 2)
        /// * `Capabilities` - Memory capabilities flags (see `xila_memory_capabilities_*` constants)
        ///
        /// # Returns
        ///
        /// - Pointer to the allocated memory block
        /// - Null if allocation fails
        ///
        /// # Errors
        ///
        /// Returns null if:
        /// - Insufficient memory available
        /// - Invalid alignment (not a power of 2)
        /// - Requested capabilities not supported
        /// - Size is too large
        ///
        /// # Examples
        ///
        /// ```c
        /// // Allocate 1024 bytes with 8-byte alignment
        /// void* ptr = xila_memory_allocate(NULL, 1024, 8, xila_memory_capabilities_none);
        ///
        /// // Allocate executable memory for code
        /// void* code_ptr = xila_memory_allocate(NULL, 4096, 4096, XILA_MEMORY_CAPABILITIES_EXECUTE);
        ///
        /// // Allocate DMA-capable memory
        /// void* dma_ptr = xila_memory_allocate(NULL, 2048, 32, xila_memory_capabilities_direct_memory_access);
        /// ```
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_memory_allocate(_: *mut c_void,
            size: usize, alignment: usize,
            capabilities: XilaMemoryCapabilities) -> *mut c_void {
            let _lock = block_on(ALLOCATION_MUTEX.lock());
            let layout =
                Allocated::get_layout_for_allocation(size, alignment);
            let capabilities =
                match CapabilityFlags::from_bits(capabilities) {
                    Some(flags) => flags,
                    None => {
                        {
                            {
                                let lvl = ::log::Level::Warn;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("xila_memory_allocate called with invalid capabilities: {0:?}, ignoring",
                                            capabilities), lvl,
                                        &("abi_definitions::memory::allocation",
                                                "abi_definitions::memory::allocation",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                        CapabilityFlags::None
                    }
                };
            let pointer =
                unsafe {
                    memory::get_instance().allocate(capabilities, layout)
                };
            let allocated =
                match Allocated::from_layout(pointer, &layout) {
                    Some(alloc) => alloc,
                    None => {
                        {
                            {
                                let lvl = ::log::Level::Warn;
                                if lvl <= ::log::STATIC_MAX_LEVEL &&
                                        lvl <= ::log::max_level() {
                                    ::log::__private_api::log({
                                            ::log::__private_api::GlobalLogger
                                        },
                                        format_args!("xila_memory_allocate failed to create allocation metadata: Size: {0}, Alignment: {1}, Capabilities: {2:?}",
                                            size, alignment, capabilities), lvl,
                                        &("abi_definitions::memory::allocation",
                                                "abi_definitions::memory::allocation",
                                                ::log::__private_api::loc()), ());
                                }
                            }
                        };
                        return null_mut();
                    }
                };
            allocated.get_user_pointer() as *mut c_void
        }
        /// Allocates a memory block with default properties.
        ///
        /// # Safety
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_memory_allocate_core(size: usize)
            -> *mut c_void {
            unsafe {
                xila_memory_allocate(null_mut(), size, align_of::<usize>(),
                    XILA_MEMORY_CAPABILITIES_NONE)
            }
        }
        /// Returns the system's memory page size.
        ///
        /// The page size is the smallest unit of memory that can be allocated by the
        /// virtual memory system. This is useful for applications that need to work
        /// with page-aligned memory or perform memory-mapped I/O operations.
        ///
        /// # Returns
        ///
        /// The page size in bytes (typically 4096 on most systems)
        ///
        /// # Examples
        ///
        /// ```c
        /// size_t page_size = xila_memory_get_page_size();
        /// printf("System page size: %zu bytes\n", page_size);
        ///
        /// // Allocate page-aligned memory
        /// void* ptr = xila_memory_allocate(NULL, page_size * 2, page_size, 0);
        /// ```
        #[unsafe(no_mangle)]
        pub extern "C" fn xila_memory_get_page_size() -> usize {
            memory::get_instance().get_page_size()
        }
        /// Flushes the data cache.
        ///
        /// This function ensures that all pending write operations in the data cache
        /// are written to main memory. This is important for cache coherency in
        /// multi-core systems or when interfacing with DMA controllers.
        ///
        /// # Safety
        ///
        /// This function is safe to call at any time, but may have performance implications
        /// as it forces cache synchronization.
        ///
        /// # Examples
        ///
        /// ```c
        /// // After writing data that will be accessed by DMA
        /// memcpy(dma_buffer, data, size);
        /// xila_memory_flush_data_cache();
        /// start_dma_transfer(dma_buffer, size);
        /// ```
        #[unsafe(no_mangle)]
        pub extern "C" fn xila_memory_flush_data_cache() {
            memory::get_instance().flush_data_cache();
        }
        /// Flushes the instruction cache for a specific memory region.
        ///
        /// This function invalidates the instruction cache for the specified memory region.
        /// This is necessary after modifying executable code to ensure the processor
        /// sees the updated instructions.
        ///
        /// # Safety
        ///
        /// - The address must point to valid memory
        /// - The memory region must not be accessed for execution during the flush operation
        /// - The size should not exceed the actual allocated memory size
        ///
        /// # Parameters
        ///
        /// * `Address` - Starting address of the memory region to flush
        /// * `Size` - Size of the memory region in bytes
        ///
        /// # Examples
        ///
        /// ```c
        /// // After writing machine code to executable memory
        /// void* code_ptr = xila_memory_allocate(NULL, 4096, 4096, XILA_MEMORY_CAPABILITIES_EXECUTE);
        /// memcpy(code_ptr, machine_code, code_size);
        /// xila_memory_flush_instruction_cache(code_ptr, code_size);
        ///
        /// // Now safe to execute the code
        /// ((void(*)())code_ptr)();
        /// ```
        #[unsafe(no_mangle)]
        pub extern "C" fn xila_memory_flush_instruction_cache(_address:
                *mut c_void, _size: usize) {
            unsafe {
                memory::get_instance().flush_instruction_cache(_address as
                        *const u8, _size);
            }
        }
    }
    mod layout {
        use core::{alloc::Layout, fmt::Debug};
        #[repr(transparent)]
        pub struct CompactLayout(usize);
        #[automatically_derived]
        #[doc(hidden)]
        unsafe impl ::core::clone::TrivialClone for CompactLayout { }
        #[automatically_derived]
        impl ::core::clone::Clone for CompactLayout {
            #[inline]
            fn clone(&self) -> CompactLayout {
                let _: ::core::clone::AssertParamIsClone<usize>;
                *self
            }
        }
        #[automatically_derived]
        impl ::core::marker::Copy for CompactLayout { }
        #[automatically_derived]
        impl ::core::marker::StructuralPartialEq for CompactLayout { }
        #[automatically_derived]
        impl ::core::cmp::PartialEq for CompactLayout {
            #[inline]
            fn eq(&self, other: &CompactLayout) -> bool { self.0 == other.0 }
        }
        #[automatically_derived]
        impl ::core::cmp::Eq for CompactLayout {
            #[doc(hidden)]
            #[coverage(off)]
            fn assert_fields_are_eq(&self) {
                let _: ::core::cmp::AssertParamIsEq<usize>;
            }
        }
        impl Debug for CompactLayout {
            fn fmt(&self, f: &mut core::fmt::Formatter<'_>)
                -> core::fmt::Result {
                f.write_fmt(format_args!("CompactLayout {{ size: {0}, alignment: {1} }}",
                        self.get_size(), self.get_alignment()))
            }
        }
        impl CompactLayout {
            /// Alignment is stored in the upper 4 bits in a log2 format
            const ALIGNMENT_BITS: u8 = 4;
            /// Size uses the remaining bits
            const SIZE_BITS: u8 =
                (size_of::<usize>() as u8 * 8) - Self::ALIGNMENT_BITS;
            const SIZE_SHIFT: usize = 0;
            const ALIGNMENT_SHIFT: usize = Self::SIZE_BITS as usize;
            const MAXIMUM_SIZE: usize = (1 << Self::SIZE_BITS) - 1;
            const MINIMUM_ALIGNMENT: usize = 1;
            const MAXIMUM_ALIGNMENT: usize =
                1 << ((1 << Self::ALIGNMENT_BITS) - 1);
            pub fn from_size_alignment(size: usize, alignment: usize)
                -> Option<Self> {
                let layout = Layout::from_size_align(size, alignment).ok()?;
                Self::from_layout(&layout)
            }
            pub const fn from_layout(layout: &Layout) -> Option<Self> {
                let size = layout.size();
                let alignment = layout.align();
                if size > Self::MAXIMUM_SIZE ||
                                alignment < Self::MINIMUM_ALIGNMENT ||
                            alignment > Self::MAXIMUM_ALIGNMENT ||
                        !alignment.is_power_of_two() {
                    return None;
                }
                let alignment_log2 = alignment.ilog2() as usize;
                let compacted =
                    (size << Self::SIZE_SHIFT) |
                        (alignment_log2 << Self::ALIGNMENT_SHIFT);
                Some(Self(compacted))
            }
            pub const fn into_layout(&self) -> Option<Layout> {
                let size = self.get_size();
                let alignment = self.get_alignment();
                match Layout::from_size_align(size, alignment) {
                    Ok(layout) => Some(layout),
                    Err(_) => None,
                }
            }
            pub const fn get_size(&self) -> usize {
                (self.0 >> Self::SIZE_SHIFT) & Self::MAXIMUM_SIZE
            }
            pub const fn get_alignment_log2(&self) -> usize {
                (self.0 >> Self::ALIGNMENT_SHIFT) &
                    ((1 << Self::ALIGNMENT_BITS) - 1)
            }
            pub const fn get_alignment(&self) -> usize {
                1 << self.get_alignment_log2()
            }
            pub const fn to_le_bytes(&self) -> [u8; size_of::<Self>()] {
                self.0.to_le_bytes()
            }
            pub fn from_le_bytes(bytes: [u8; size_of::<Self>()])
                -> Option<Self> {
                let layout = Self(usize::from_le_bytes(bytes));
                if layout.get_size() > Self::MAXIMUM_SIZE ||
                            layout.get_alignment() < Self::MINIMUM_ALIGNMENT ||
                        layout.get_alignment() > Self::MAXIMUM_ALIGNMENT {
                    return None;
                }
                Some(layout)
            }
        }
    }
    mod manipulation {
        use core::{
            cmp::Ordering, ffi::{c_int, c_void},
            ptr::{copy, copy_nonoverlapping, write_bytes},
            slice,
        };
        /// Copy memory from source to destination
        ///
        /// # Safety
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_memory_copy(destination: *mut c_void,
            source: *const c_void, size: usize) -> *mut c_void {
            if destination.is_null() || source.is_null() || size == 0 {
                return destination;
            }
            unsafe {
                copy_nonoverlapping(source as *const u8,
                    destination as *mut u8, size);
            }
            destination
        }
        /// Compare memory blocks
        ///
        /// # Safety
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_memory_compare(ptr1: *const c_void,
            ptr2: *const c_void, num: usize) -> c_int {
            if ptr1.is_null() || ptr2.is_null() || num == 0 { return 0; }
            let bytes1 =
                unsafe { slice::from_raw_parts(ptr1 as *const u8, num) };
            let bytes2 =
                unsafe { slice::from_raw_parts(ptr2 as *const u8, num) };
            match bytes1.cmp(bytes2) {
                Ordering::Less => -1,
                Ordering::Equal => 0,
                Ordering::Greater => 1,
            }
        }
        /// Move memory from source to destination (handles overlapping regions)
        ///
        /// # Safety
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_memory_move(destination: *mut c_void,
            source: *const c_void, size: usize) -> *mut c_void {
            if destination.is_null() || source.is_null() || size == 0 {
                return destination;
            }
            unsafe {
                copy(source as *const u8, destination as *mut u8, size);
            }
            destination
        }
        /// Set memory to a specific value
        ///
        /// # Safety
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_memory_set(ptr: *mut c_void,
            value: c_int, num: usize) -> *mut c_void {
            if ptr.is_null() || num == 0 { return ptr; }
            unsafe { write_bytes(ptr as *mut u8, value as u8, num); }
            ptr
        }
    }
    pub use allocated::*;
    pub use allocation::*;
    pub use layout::*;
    pub use manipulation::*;
}
mod string {
    use core::{
        cmp::{Ordering, min},
        ffi::{c_char, c_int},
        ptr::null_mut, slice,
    };
    use alloc::str;
    use crate::memory::xila_memory_allocate_core;
    /// Helper function to convert C string to Rust str for parsing
    unsafe fn c_str_to_str(ptr: *const c_char) -> Result<&'static str, ()> {
        unsafe {
            if ptr.is_null() { return Err(()); }
            let len = xila_string_get_length(ptr);
            let slice = slice::from_raw_parts(ptr as *const u8, len);
            core::str::from_utf8(slice).map_err(|_| ())
        }
    }
    /// Get the length of a null-terminated string
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_get_length(str: *const c_char)
        -> usize {
        unsafe {
            if str.is_null() { return 0; }
            let mut len = 0;
            let mut ptr = str;
            while *ptr != 0 { len += 1; ptr = ptr.add(1); }
            len
        }
    }
    /// Get the length of a null-terminated string with maximum length
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_get_length_bounded(str:
            *const c_char, maxlen: usize) -> usize {
        unsafe {
            if str.is_null() { return 0; }
            let mut len = 0;
            let mut ptr = str;
            while len < maxlen && *ptr != 0 { len += 1; ptr = ptr.add(1); }
            len
        }
    }
    /// Compare two null-terminated strings
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_compare(str1: *const c_char,
        str2: *const c_char) -> c_int {
        unsafe {
            if str1.is_null() || str2.is_null() {
                return if str1.is_null() && str2.is_null() {
                        0
                    } else if str1.is_null() { -1 } else { 1 };
            }
            let len1 = xila_string_get_length(str1);
            let len2 = xila_string_get_length(str2);
            let slice1 = slice::from_raw_parts(str1 as *const u8, len1);
            let slice2 = slice::from_raw_parts(str2 as *const u8, len2);
            match slice1.cmp(slice2) {
                Ordering::Less => -1,
                Ordering::Equal => 0,
                Ordering::Greater => 1,
            }
        }
    }
    /// Compare two strings up to n characters
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_compare_bounded(str1: *const c_char,
        str2: *const c_char, num: usize) -> c_int {
        unsafe {
            if str1.is_null() || str2.is_null() || num == 0 {
                return if str1.is_null() && str2.is_null() {
                        0
                    } else if str1.is_null() { -1 } else { 1 };
            }
            let len1 = min(xila_string_get_length(str1), num);
            let len2 = min(xila_string_get_length(str2), num);
            let min_len = min(len1, len2);
            let slice1 = slice::from_raw_parts(str1 as *const u8, min_len);
            let slice2 = slice::from_raw_parts(str2 as *const u8, min_len);
            use core::cmp::Ordering;
            match slice1.cmp(slice2) {
                Ordering::Less => -1,
                Ordering::Greater => 1,
                Ordering::Equal => {
                    match len1.cmp(&len2) {
                        Ordering::Less => -1,
                        Ordering::Equal => 0,
                        Ordering::Greater => 1,
                    }
                }
            }
        }
    }
    /// Copy string from source to destination
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_copy(destination: *mut c_char,
        source: *const c_char) -> *mut c_char {
        if destination.is_null() || source.is_null() { return destination; }
        let mut dst_ptr = destination;
        let mut src_ptr = source;
        unsafe {
            while *src_ptr != 0 {
                *dst_ptr = *src_ptr;
                dst_ptr = dst_ptr.add(1);
                src_ptr = src_ptr.add(1);
            }
            *dst_ptr = 0;
        }
        destination
    }
    /// Copy string from source to destination with maximum length
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_copy_bounded(destination:
            *mut c_char, source: *const c_char, num: usize) -> *mut c_char {
        unsafe {
            if destination.is_null() || source.is_null() || num == 0 {
                return destination;
            }
            let mut dst_ptr = destination;
            let mut src_ptr = source;
            let mut count = 0;
            while count < num && *src_ptr != 0 {
                *dst_ptr = *src_ptr;
                dst_ptr = dst_ptr.add(1);
                src_ptr = src_ptr.add(1);
                count += 1;
            }
            while count < num {
                *dst_ptr = 0;
                dst_ptr = dst_ptr.add(1);
                count += 1;
            }
            destination
        }
    }
    /// Tokenize a string using delimiters
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_tokenize(_string: *mut c_char,
        delimiters: *const c_char) -> *mut c_char {
        if delimiters.is_null() { return null_mut(); }
        null_mut()
    }
    /// Find substring in a string
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_find_substring(haystack:
            *const c_char, needle: *const c_char) -> *mut c_char {
        unsafe {
            if haystack.is_null() || needle.is_null() { return null_mut(); }
            let needle_len = xila_string_get_length(needle);
            if needle_len == 0 { return haystack as *mut c_char; }
            let haystack_len = xila_string_get_length(haystack);
            if needle_len > haystack_len { return null_mut(); }
            let haystack_slice =
                slice::from_raw_parts(haystack as *const u8, haystack_len);
            let needle_slice =
                slice::from_raw_parts(needle as *const u8, needle_len);
            for (i, window) in haystack_slice.windows(needle_len).enumerate()
                {
                if window == needle_slice {
                    return haystack.add(i) as *mut c_char;
                }
            }
            null_mut()
        }
    }
    /// Convert string to double
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_to_double(nptr: *const c_char,
        endptr: *mut *mut c_char) -> f64 {
        unsafe {
            if nptr.is_null() {
                if !endptr.is_null() { *endptr = nptr as *mut c_char; }
                return 0.0;
            }
            if let Ok(s) = c_str_to_str(nptr) {
                let trimmed = s.trim_start();
                if let Ok(value) = trimmed.parse::<f64>() {
                    if !endptr.is_null() {
                        let consumed =
                            s.len() - trimmed.len() +
                                trimmed.chars().take_while(|c| !c.is_whitespace()).count();
                        *endptr = nptr.add(consumed) as *mut c_char;
                    }
                    return value;
                }
            }
            if !endptr.is_null() { *endptr = nptr as *mut c_char; }
            0.0
        }
    }
    /// Case-insensitive string comparison up to n characters
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_compare_case_insensitive_bounded(str1:
            *const c_char, str2: *const c_char, num: usize) -> c_int {
        unsafe {
            if str1.is_null() || str2.is_null() || num == 0 { return 0; }
            let len1 = min(xila_string_get_length(str1), num);
            let len2 = min(xila_string_get_length(str2), num);
            let min_len = min(len1, len2);
            let slice1 = slice::from_raw_parts(str1 as *const u8, min_len);
            let slice2 = slice::from_raw_parts(str2 as *const u8, min_len);
            for (a, b) in slice1.iter().zip(slice2.iter()) {
                let lower_a = a.to_ascii_lowercase();
                let lower_b = b.to_ascii_lowercase();
                if lower_a != lower_b {
                    return if lower_a < lower_b { -1 } else { 1 };
                }
            }
            use core::cmp::Ordering;
            match len1.cmp(&len2) {
                Ordering::Less => -1,
                Ordering::Equal => 0,
                Ordering::Greater => 1,
            }
        }
    }
    /// Convert string to unsigned long
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_to_unsigned_long(nptr: *const c_char,
        endptr: *mut *mut c_char, base: c_int) -> u64 {
        unsafe {
            if nptr.is_null() {
                if !endptr.is_null() { *endptr = nptr as *mut c_char; }
                return 0;
            }
            if let Ok(s) = c_str_to_str(nptr) {
                let trimmed = s.trim_start();
                let radix =
                    if base == 0 {
                        if trimmed.starts_with("0x") || trimmed.starts_with("0X") {
                            16
                        } else if trimmed.starts_with("0") && trimmed.len() > 1 {
                            8
                        } else { 10 }
                    } else { base as u32 };
                if (2..=36).contains(&radix) {
                    let parse_str =
                        if radix == 16 &&
                                (trimmed.starts_with("0x") || trimmed.starts_with("0X")) {
                            &trimmed[2..]
                        } else { trimmed };
                    if let Ok(value) = u64::from_str_radix(parse_str, radix) {
                        if !endptr.is_null() {
                            let consumed =
                                s.len() - trimmed.len() +
                                        if radix == 16 && parse_str != trimmed { 2 } else { 0 } +
                                    parse_str.chars().take_while(|c|
                                                c.is_ascii_alphanumeric()).count();
                            *endptr = nptr.add(consumed) as *mut c_char;
                        }
                        return value;
                    }
                }
            }
            if !endptr.is_null() { *endptr = nptr as *mut c_char; }
            0
        }
    }
    /// Find character in string
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_find_character(s: *const c_char,
        c: c_int) -> *mut c_char {
        unsafe {
            if s.is_null() { return null_mut(); }
            let target = c as u8;
            let len = xila_string_get_length(s);
            let slice = slice::from_raw_parts(s as *const u8, len + 1);
            if let Some(pos) = slice.iter().position(|&byte| byte == target) {
                return s.add(pos) as *mut c_char;
            }
            null_mut()
        }
    }
    /// Convert string to float
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_to_float(nptr: *const c_char,
        endptr: *mut *mut c_char) -> f32 {
        unsafe {
            if nptr.is_null() {
                if !endptr.is_null() { *endptr = nptr as *mut c_char; }
                return 0.0;
            }
            if let Ok(s) = c_str_to_str(nptr) {
                let trimmed = s.trim_start();
                if let Ok(value) = trimmed.parse::<f32>() {
                    if !endptr.is_null() {
                        let consumed =
                            s.len() - trimmed.len() +
                                trimmed.chars().take_while(|c| !c.is_whitespace()).count();
                        *endptr = nptr.add(consumed) as *mut c_char;
                    }
                    return value;
                }
            }
            if !endptr.is_null() { *endptr = nptr as *mut c_char; }
            0.0
        }
    }
    /// Get length of initial segment of string that consists of reject characters
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_span_complement(s: *const c_char,
        reject: *const c_char) -> usize {
        unsafe {
            if s.is_null() || reject.is_null() { return 0; }
            let s_len = xila_string_get_length(s);
            let reject_len = xila_string_get_length(reject);
            let s_slice = slice::from_raw_parts(s as *const u8, s_len);
            let reject_slice =
                slice::from_raw_parts(reject as *const u8, reject_len);
            s_slice.iter().position(|&byte|
                        reject_slice.contains(&byte)).unwrap_or(s_len)
        }
    }
    /// Get length of initial segment of string that consists of accept characters
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_span(s: *const c_char,
        accept: *const c_char) -> usize {
        unsafe {
            if s.is_null() || accept.is_null() { return 0; }
            let s_len = xila_string_get_length(s);
            let accept_len = xila_string_get_length(accept);
            let s_slice = slice::from_raw_parts(s as *const u8, s_len);
            let accept_slice =
                slice::from_raw_parts(accept as *const u8, accept_len);
            s_slice.iter().position(|&byte|
                        !accept_slice.contains(&byte)).unwrap_or(s_len)
        }
    }
    /// Convert string to unsigned long long
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_to_unsigned_long_long(nptr:
            *const c_char, endptr: *mut *mut c_char, base: c_int) -> u64 {
        unsafe { xila_string_to_unsigned_long(nptr, endptr, base) }
    }
    /// Duplicate a string
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_duplicate(string: *const c_char)
        -> *mut c_char {
        if string.is_null() { return null_mut(); }
        let new_string =
            unsafe {
                let length = xila_string_get_length(string);
                xila_memory_allocate_core(length + 1)
            };
        if new_string.is_null() { return null_mut(); }
        unsafe { xila_string_copy(new_string as *mut c_char, string); }
        new_string as *mut c_char
    }
    /// Duplicate a string up to a maximum length
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_duplicate_bounded(string:
            *const c_char, max_length: usize) -> *mut c_char {
        if string.is_null() || max_length == 0 { return null_mut(); }
        let length =
            unsafe { xila_string_get_length_bounded(string, max_length) };
        let new_string = unsafe { xila_memory_allocate_core(length + 1) };
        if new_string.is_null() { return null_mut(); }
        unsafe {
            xila_string_copy_bounded(new_string as *mut c_char, string,
                length);
        }
        new_string as *mut c_char
    }
    /// Parse integer from string
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_parse_integer(string: *const c_char)
        -> c_int {
        unsafe {
            if string.is_null() { return 0; }
            if let Ok(s) = c_str_to_str(string) {
                if let Ok(value) = s.trim().parse::<c_int>() { return value; }
            }
            0
        }
    }
    /// Concatenate source string to destination string
    ///
    /// # Safety
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_string_concatenate(destination: *mut c_char,
        mut source: *const c_char) -> *mut c_char {
        if destination.is_null() || source.is_null() { return destination; }
        let mut destination_end = destination;
        unsafe {
            while *destination_end != 0 {
                destination_end = destination_end.add(1);
            }
        }
        unsafe {
            while *source != 0 {
                *destination_end = *source;
                destination_end = destination_end.add(1);
                source = source.add(1);
            }
            *destination_end = 0;
        }
        destination
    }
}
mod task {
    mod condition_variable {
        use core::ffi::c_int;
        use crate::{XilaTime, XilaTimeClockIdentifier};
        use super::RawMutex;
        pub type XilaConditionVariable = usize;
        pub type XilaConditionVariableAttribute = usize;
        /// This function is used to create a condition variable.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_new(_condition_variable:
                *mut XilaConditionVariable) -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to initialize a condition variable.
        ///
        /// # Safety
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_initialize(_condition_variable:
                *mut XilaConditionVariable,
            _attribute: *const XilaConditionVariableAttribute) -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to delete a condition variable.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_remove(_condition_variable:
                *mut XilaConditionVariable) -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to wait a condition variable.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_wait(_condition_variable:
                *mut XilaConditionVariable, _mutex: *mut RawMutex) -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to try wait a condition variable.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_try_wait(_condition_variable:
                *mut XilaConditionVariable, _mutex: *mut RawMutex,
            _time: XilaTime) -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to signal a condition variable.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_signal(_condition_variable:
                *mut XilaConditionVariable) -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to broadcast a condition variable.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_broadcast(_condition_variable:
                *mut XilaConditionVariable) -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to create a condition variable attribute.
        ///
        /// # Safety
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_attribute_initialize(_attribute:
                *mut XilaConditionVariableAttribute) -> c_int {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to set the clock of a condition variable attribute.
        ///
        /// # Safety
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_attribute_set_clock(_attribute:
                *mut XilaConditionVariableAttribute,
            _clock: XilaTimeClockIdentifier) -> c_int {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to destroy a condition variable attribute.
        ///
        /// # Safety
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_condition_variable_attribute_destroy(_attribute:
                *mut XilaConditionVariableAttribute) -> c_int {
            ::core::panicking::panic("not yet implemented")
        }
    }
    mod mutex {
        use core::{
            mem::{align_of, size_of},
            ptr::drop_in_place,
        };
        use synchronization::blocking_mutex::{
            Mutex, raw::CriticalSectionRawMutex,
        };
        use task::TaskIdentifier;
        use crate::XilaTaskIdentifier;
        struct MutexState {
            task: Option<TaskIdentifier>,
            lock_count: u32,
        }
        #[automatically_derived]
        impl ::core::fmt::Debug for MutexState {
            #[inline]
            fn fmt(&self, f: &mut ::core::fmt::Formatter)
                -> ::core::fmt::Result {
                ::core::fmt::Formatter::debug_struct_field2_finish(f,
                    "MutexState", "task", &self.task, "lock_count",
                    &&self.lock_count)
            }
        }
        #[automatically_derived]
        #[doc(hidden)]
        unsafe impl ::core::clone::TrivialClone for MutexState { }
        #[automatically_derived]
        impl ::core::clone::Clone for MutexState {
            #[inline]
            fn clone(&self) -> MutexState {
                let _:
                        ::core::clone::AssertParamIsClone<Option<TaskIdentifier>>;
                let _: ::core::clone::AssertParamIsClone<u32>;
                *self
            }
        }
        #[automatically_derived]
        impl ::core::marker::Copy for MutexState { }
        #[automatically_derived]
        impl ::core::default::Default for MutexState {
            #[inline]
            fn default() -> MutexState {
                MutexState {
                    task: ::core::default::Default::default(),
                    lock_count: ::core::default::Default::default(),
                }
            }
        }
        pub struct RawMutex {
            mutex: Mutex<CriticalSectionRawMutex, MutexState>,
            recursive: bool,
        }
        impl RawMutex {
            pub fn new(recursive: bool) -> Self {
                Self { mutex: Mutex::new(MutexState::default()), recursive }
            }
            pub fn is_valid_pointer(pointer: *const RawMutex) -> bool {
                !pointer.is_null() &&
                    (pointer as usize).is_multiple_of(align_of::<Self>())
            }
            /// Transforms a pointer to a reference.
            ///
            /// # Safety
            ///
            /// This function is unsafe because it dereferences a raw pointer.
            /// The caller must ensure the pointer is valid and points to properly initialized memory.
            pub unsafe fn from_pointer<'a>(pointer: *const RawMutex)
                -> Option<&'a Self> {
                unsafe {
                    if !Self::is_valid_pointer(pointer) { return None; }
                    Some(&*pointer)
                }
            }
            /// Transforms a mutable pointer to a mutable reference.
            ///
            /// # Safety
            ///
            /// This function is unsafe because it dereferences a raw pointer.
            /// The caller must ensure the pointer is valid and points to properly initialized memory.
            pub unsafe fn from_mutable_pointer<'a>(pointer: *mut RawMutex)
                -> Option<&'a mut Self> {
                unsafe {
                    if !Self::is_valid_pointer(pointer) { return None; }
                    Some(&mut *pointer)
                }
            }
            pub fn lock(&self, task: TaskIdentifier) -> bool {
                unsafe {
                    self.mutex.lock_mut(|state|
                            {
                                if let Some(owner) = state.task {
                                    if owner == task && self.recursive {
                                        state.lock_count += 1;
                                        return true;
                                    }
                                    return false;
                                }
                                state.task = Some(task);
                                state.lock_count = 1;
                                true
                            })
                }
            }
            pub fn unlock(&self, task: TaskIdentifier) -> bool {
                unsafe {
                    self.mutex.lock_mut(|state|
                            {
                                if let Some(owner) = state.task && owner == task {
                                    if self.recursive && state.lock_count > 1 {
                                        state.lock_count -= 1;
                                    } else { state.task = None; state.lock_count = 0; }
                                    return true;
                                }
                                false
                            })
                }
            }
        }
        #[unsafe(no_mangle)]
        pub static RAW_MUTEX_SIZE: usize = size_of::<RawMutex>();
        /// This function is used to initialize a mutex.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        ///
        /// # Errors
        ///
        /// This function may return an error if the mutex is not initialized.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_initialize_mutex(mutex: *mut RawMutex)
            -> bool {
            unsafe {
                if mutex.is_null() { return false; }
                if !(mutex as usize).is_multiple_of(align_of::<RawMutex>()) {
                    return false;
                }
                mutex.write(RawMutex::new(false));
                true
            }
        }
        /// Initialize a recursive mutex.
        ///
        /// # Safety
        ///
        /// The caller must ensure:
        /// - `mutex` points to valid, uninitialized memory
        /// - The memory is properly aligned for `Raw_mutex_type`
        /// - The memory will remain valid for the lifetime of the mutex
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_initialize_recursive_mutex(mutex:
                *mut RawMutex) -> bool {
            unsafe {
                if mutex.is_null() { return false; }
                if !(mutex as usize).is_multiple_of(align_of::<RawMutex>()) {
                    return false;
                }
                mutex.write(RawMutex::new(true));
                true
            }
        }
        /// Lock a mutex (blocking).
        ///
        /// # Safety
        ///
        /// The caller must ensure:
        /// - `mutex` points to a valid, initialized `Raw_mutex_type`
        /// - The mutex remains valid for the duration of the call
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_lock_mutex(mutex: *mut RawMutex,
            task: XilaTaskIdentifier) -> bool {
            unsafe {
                let mutex =
                    match RawMutex::from_mutable_pointer(mutex) {
                        Some(mutex) => mutex,
                        None => return false,
                    };
                mutex.lock(task.into())
            }
        }
        /// Unlock a mutex (blocking).
        ///
        /// # Safety
        ///
        /// The caller must ensure:
        /// - `mutex` points to a valid, initialized `Raw_mutex_type`
        /// - The mutex remains valid for the duration of the call
        /// - The current task owns the mutex
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_unlock_mutex(mutex: *mut RawMutex,
            task: XilaTaskIdentifier) -> bool {
            unsafe {
                let mutex =
                    match RawMutex::from_mutable_pointer(mutex) {
                        Some(mutex) => mutex,
                        None => return false,
                    };
                mutex.unlock(task.into())
            }
        }
        /// Destroy a mutex.
        ///
        /// # Safety
        ///
        /// The caller must ensure:
        /// - `mutex` points to a valid, initialized `Raw_mutex_type` allocated with Box
        /// - The mutex is not currently locked
        /// - No other threads are waiting on the mutex
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_destroy_mutex(mutex: *mut RawMutex)
            -> bool {
            unsafe {
                let mutex =
                    match RawMutex::from_mutable_pointer(mutex) {
                        Some(mutex) => mutex,
                        None => return false,
                    };
                drop_in_place(mutex);
                true
            }
        }
    }
    mod rwlock {
        use core::{
            mem::{align_of, size_of},
            ptr::drop_in_place,
        };
        use synchronization::blocking_mutex::{
            Mutex, raw::CriticalSectionRawMutex,
        };
        pub struct RawRwLock {
            /// Mutex to protect the lock state.
            ///
            /// The lock state is represented as follows:
            /// - 0: Unlocked
            /// - 1: Write locked (no readers allowed)
            /// - 2 or more: Read locked (number of readers)
            mutex: Mutex<CriticalSectionRawMutex, usize>,
        }
        impl Default for RawRwLock {
            fn default() -> Self { Self::new() }
        }
        impl RawRwLock {
            const READING: usize = 2;
            const WRITING: usize = 1;
            const UNLOCKED: usize = 0;
            pub fn new() -> Self {
                Self { mutex: Mutex::new(Self::UNLOCKED) }
            }
            pub fn is_valid_pointer(pointer: *const RawRwLock) -> bool {
                !pointer.is_null() &&
                    (pointer as usize).is_multiple_of(align_of::<Self>())
            }
            /// Transforms a pointer to a reference.
            ///
            /// # Safety
            ///
            /// This function is unsafe because it dereferences a raw pointer.
            /// The caller must ensure the pointer is valid and points to properly initialized memory.
            pub unsafe fn from_pointer<'a>(pointer: *const RawRwLock)
                -> Option<&'a Self> {
                unsafe {
                    if !Self::is_valid_pointer(pointer) { return None; }
                    Some(&*pointer)
                }
            }
            /// Transforms a mutable pointer to a mutable reference.
            ///
            /// # Safety
            ///
            /// This function is unsafe because it dereferences a raw pointer.
            /// The caller must ensure the pointer is valid and points to properly initialized memory.
            pub unsafe fn from_mutable_pointer<'a>(pointer: *mut RawRwLock)
                -> Option<&'a mut Self> {
                unsafe {
                    if !Self::is_valid_pointer(pointer) { return None; }
                    Some(&mut *pointer)
                }
            }
            pub fn read(&self) -> bool {
                unsafe {
                    self.mutex.lock_mut(|state|
                            {
                                match *state {
                                    Self::WRITING => return false,
                                    Self::UNLOCKED => *state = Self::READING,
                                    _ => *state += 1,
                                }
                                true
                            })
                }
            }
            pub fn write(&self) -> bool {
                unsafe {
                    self.mutex.lock_mut(|state|
                            {
                                if *state != Self::UNLOCKED { return false; }
                                *state = Self::WRITING;
                                true
                            })
                }
            }
            pub fn unlock(&self) -> bool {
                unsafe {
                    self.mutex.lock_mut(|state|
                            {
                                match *state {
                                    Self::UNLOCKED => false,
                                    Self::WRITING => { *state = Self::UNLOCKED; true }
                                    n if n >= 2 => {
                                        *state -= 1;
                                        if *state == Self::WRITING { *state = Self::UNLOCKED; }
                                        true
                                    }
                                    _ => false,
                                }
                            })
                }
            }
        }
        #[unsafe(no_mangle)]
        pub static RAW_RWLOCK_SIZE: usize = size_of::<RawRwLock>();
        /// This function is used to initialize a rwlock.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        ///
        /// # Errors
        ///
        /// This function may return an error if the rwlock is not initialized.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_initialize_rwlock(rwlock:
                *mut RawRwLock) -> bool {
            unsafe {
                if rwlock.is_null() { return false; }
                if !(rwlock as usize).is_multiple_of(align_of::<RawRwLock>())
                    {
                    return false;
                }
                rwlock.write(RawRwLock::new());
                true
            }
        }
        /// Read lock a rwlock.
        ///
        /// # Safety
        ///
        /// The caller must ensure:
        /// - `rwlock` points to a valid, initialized `Raw_rwlock_type`
        /// - The rwlock remains valid for the duration of the call
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_read_rwlock(rwlock: *mut RawRwLock)
            -> bool {
            unsafe {
                let rwlock =
                    match RawRwLock::from_mutable_pointer(rwlock) {
                        Some(rwlock) => rwlock,
                        None => return false,
                    };
                rwlock.read()
            }
        }
        /// Write lock a rwlock.
        ///
        /// # Safety
        ///
        /// The caller must ensure:
        /// - `rwlock` points to a valid, initialized `Raw_rwlock_type`
        /// - The rwlock remains valid for the duration of the call
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_write_rwlock(rwlock: *mut RawRwLock)
            -> bool {
            unsafe {
                let rwlock =
                    match RawRwLock::from_mutable_pointer(rwlock) {
                        Some(rwlock) => rwlock,
                        None => return false,
                    };
                rwlock.write()
            }
        }
        /// Unlock a rwlock.
        ///
        /// # Safety
        ///
        /// The caller must ensure:
        /// - `rwlock` points to a valid, initialized `Raw_rwlock_type`
        /// - The rwlock remains valid for the duration of the call
        /// - The current task owns the lock (either read or write)
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_unlock_rwlock(rwlock: *mut RawRwLock)
            -> bool {
            unsafe {
                let rwlock =
                    match RawRwLock::from_mutable_pointer(rwlock) {
                        Some(rwlock) => rwlock,
                        None => return false,
                    };
                rwlock.unlock()
            }
        }
        /// Destroy a rwlock.
        ///
        /// # Safety
        ///
        /// The caller must ensure:
        /// - `rwlock` points to a valid, initialized `Raw_rwlock_type` allocated with Box
        /// - The rwlock is not currently locked
        /// - No other threads are waiting on the rwlock
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_destroy_rwlock(rwlock: *mut RawRwLock)
            -> bool {
            unsafe {
                let _ =
                    match RawRwLock::from_mutable_pointer(rwlock) {
                        Some(rw_lock) => rw_lock,
                        None => return false,
                    };
                drop_in_place(rwlock);
                true
            }
        }
    }
    mod semaphore {
        use core::ffi::{c_char, c_int};
        use crate::{XilaFileSystemAccess, XilaFileSystemOpen};
        pub struct XilaSemaphore;
        /// This function is used to create a semaphore.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_semaphore_open(_name: *const c_char,
            _open: XilaFileSystemOpen, _mode: XilaFileSystemAccess,
            _value: isize) -> *mut XilaSemaphore {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to close a semaphore.
        #[unsafe(no_mangle)]
        pub extern "C" fn xila_semaphore_close(_semaphore: *mut XilaSemaphore)
            -> u32 {
            let _ = _semaphore;
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to wait a semaphore.
        #[unsafe(no_mangle)]
        pub extern "C" fn xila_semaphore_wait(_semaphore: *mut XilaSemaphore)
            -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to try wait a semaphore.
        #[unsafe(no_mangle)]
        pub extern "C" fn xila_semaphore_try_wait(_semaphore:
                *mut XilaSemaphore) -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to post a semaphore.
        #[unsafe(no_mangle)]
        pub extern "C" fn xila_semaphore_post(_semaphore: *mut XilaSemaphore)
            -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to get the value of a semaphore.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_semaphore_get_value(_semaphore:
                *mut XilaSemaphore, _value: *mut c_int) -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
        /// This function is used to get the value of a semaphore.
        ///
        /// # Safety
        ///
        /// This function is unsafe because it dereferences raw pointers.
        #[unsafe(no_mangle)]
        pub unsafe extern "C" fn xila_semaphore_remove(_name: *const c_char)
            -> u32 {
            ::core::panicking::panic("not yet implemented")
        }
    }
    pub use condition_variable::*;
    pub use mutex::*;
    pub use rwlock::*;
    pub use semaphore::*;
    pub type XilaTaskIdentifier = usize;
}
mod time {
    use core::ffi::{c_int, c_void};
    use time::get_instance;
    pub type XilaTime = u64;
    #[repr(C)]
    pub struct XilaTimePrecision {
        seconds: XilaTime,
        nanoseconds: u32,
    }
    #[repr(u8)]
    pub enum XilaTimeClockIdentifier { Monotonic, Realtime, }
    #[repr(u8)]
    pub enum XilaTimerFlags { Absolute, }
    /// Retrieve the current time since the system startup in microseconds.
    ///
    /// # Returns
    ///
    /// The current time since the system startup in microseconds.
    #[unsafe(no_mangle)]
    pub extern "C" fn xila_time_get_time_since_startup_microseconds() -> u64 {
        get_instance().get_current_time_since_startup().unwrap_or_default().as_micros()
            as u64
    }
    #[unsafe(no_mangle)]
    pub extern "C" fn xila_time_get_cpu() -> u64 {
        ::core::panicking::panic("not yet implemented")
    }
    /// Retrieve the current time since the system startup in milliseconds.
    ///
    /// # Safety
    ///
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub unsafe extern "C" fn xila_time_get_resolution(_clock_identifier:
            XilaTimeClockIdentifier, _resolution: *mut XilaTime) -> u32 {
        ::core::panicking::panic("not yet implemented")
    }
    /// Retrieve the current time since the system startup in milliseconds.
    ///
    /// # Safety
    ///
    /// This function is unsafe because it dereferences raw pointers.
    #[unsafe(no_mangle)]
    pub extern "C" fn xila_time_get_time(_clock_identifier:
            XilaTimeClockIdentifier, _precision: u64, _time: *mut XilaTime)
        -> u32 {
        ::core::panicking::panic("not yet implemented")
    }
    #[unsafe(no_mangle)]
    pub extern "C" fn xila_time_nano_sleep(_clock_identifier:
            XilaTimeClockIdentifier, _flags: XilaTimerFlags,
        _time: *const c_void, _remaining: *mut XilaTime) -> c_int {
        0
    }
    #[unsafe(no_mangle)]
    pub extern "C" fn xila_time_dummy(_: XilaTimePrecision) -> XilaTime { 0 }
}
mod user {
    pub type XilaUserIdentifier = u16;
    pub type XilaGroupIdentifier = u16;
}
mod common {
    macro_rules! abi_function {
        ($(#[$meta:meta])* fn $name:ident($($arg:ident : $ty:ty),* $(,)?) ->
        XilaFileSystemResult $body:block) =>
        {
            $(#[$meta])* #[unsafe(no_mangle)] pub unsafe extern "C" fn
            $name($($arg : $ty),*) -> XilaFileSystemResult
            { into_result(|| unsafe { $body }) }
        };
    }
}
pub use file_system::*;
pub use memory::*;
pub use string::*;
pub use task::*;
pub use time::*;
pub use user::*;
pub use common::*;
