[workspace]

[package]
name = "File_system_bindings_WASM_test"
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = 's'
strip = true

[dependencies]
Binding_tool = { path = "../../../../../Binding_tool", features = ["WASM"] }
